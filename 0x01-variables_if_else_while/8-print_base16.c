#include <stdio.h>
#include <time.h>
/**
*main - lenguages exercises c
*Return: 0
*/

int main(void)
{
int num;
int abc;
for (num = '0'; num <= '9'; num++)
putchar (num);
for (abc = 'a'; abc <= 'f'; abc++)
putchar (abc);
putchar ('\n');
return (0);
}
