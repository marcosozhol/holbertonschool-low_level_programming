#include <stdio.h>
#include <time.h>
/**
*main - lenguages exercises c
*Return: 0
*/

int main(void)
{
int abc;
int ABC;
for (abc = 'a'; abc <= 'z'; abc++)
putchar (abc);
for (ABC = 'A'; ABC <= 'Z'; ABC++)
putchar (ABC);
putchar ('\n');
return (0);
}
